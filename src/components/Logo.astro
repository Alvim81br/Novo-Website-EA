---
// Logomarca oficial da English Academy: escudo (WebP otimizado em /public/logo-ea.webp)
// com wordmark na tipografia da marca — "ENGLISH" em Cinzel (serifada clássica,
// branca) e "ACADEMY" em Nunito Bold (dourada).
interface Props {
  class?: string;
}
const { class: className = 'h-10' } = Astro.props;

// "ACADEMY" é renderizada letra a letra e distribuída (justify-between) para
// ocupar exatamente a mesma largura de "ENGLISH", independente da métrica da
// fonte — assim as duas linhas ficam sempre alinhadas nas duas pontas.
const academy = [...'ACADEMY'];
---

<span class={`flex items-center gap-3 ${className}`}>
  <img
    src="/logo-ea.webp"
    alt="English Academy"
    class="h-full w-auto drop-shadow-sm"
    width="288"
    height="320"
    loading="eager"
    decoding="async"
    onerror="this.style.display='none'"
  />
  <span
    class="flex translate-y-[0.055em] flex-col items-stretch justify-center leading-none"
    aria-hidden="true"
  >
    <span class="font-brand text-[1.2em] font-bold text-white">ENGLISH</span>
    <span class="font-logo mt-[-0.2em] flex w-full justify-between text-[1em] font-bold text-[#CFA13F]">
      {academy.map((letra) => <span>{letra}</span>)}
    </span>
  </span>
</span>
